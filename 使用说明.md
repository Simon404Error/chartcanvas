# ChartCanvas 可视化画图（桌面版）

一个**通用可视化画图桌面工具**：打开任意 **Excel / CSV** 数据，即可在左侧选择字段、中间实时预览、右侧深度自定义样式，绘制**多种统计图**。借鉴 enterprise-analysis 项目的 Excel 解析 + 交互面板思路，但做成独立、通用的桌面应用（非网页版）。

## 快速开始

1. 双击 **`启动ChartCanvas.bat`**（会弹出黑色控制台，关闭它即退出程序）。
2. 点击顶部 **「✨ 加载示例数据」** 立即体验，或点 **「📂 打开 Excel / CSV」** 选择你自己的数据文件（支持 `.xlsx .xls .csv .tsv .txt`）。
3. 左侧选择字段 → 选图表类型 → 右侧调样式，图实时更新。
4. 点 **「⬇ 导出 PNG / SVG」** 保存图片。

> 首次打开示例数据后自动生成了**默认柱状图**。数据不离开本机、完全离线运行。

## 支持的图表类型（11 种）

| 类型 | 说明 |
|---|---|
| 柱状图 | 分组 / 堆叠 / 横向，显示数值，X轴标签旋转 |
| 折线图 | 可平滑曲线、显示数据点、对数Y轴，多系列 |
| 面积图 | 默认堆叠面积，可折线堆叠 |
| 散点气泡 | 支持按第三个数值列控制点大小（气泡） |
| 饼图环图 | 可调空心比例、标注样式 |
| 直方图 | 分箱数、累计分布 |
| 箱线图 | 显示全部点 / 横向 |
| 热力图 | 分类×分类矩阵，多种色带 |
| 雷达图 | 多维度对比，可填充 |
| 帕累托 | 柱状 + 累计占比折线（二Y轴） |
| 统计概要 | 分组字段的描述统计表格 |

## 高度自定义（右侧面板）

- **常规**：标题、宽高、字体、字号、配色主题、背景（白/浅/深）
- **按图类型动态的细节项**：堆叠、横向、显示数值、小数位、轴标题、图例横排、对数轴、透明度、线宽、曲线平滑、空心环、分箱、色带、标签旋转、气泡放大等
- 图内 Plotly 交互：缩放、平移、悬停读数、模式栏、双击复位

## 数据使用要点

- X 轴：建议选分类/时间列；Y 轴：选数值列（可多选形成多系列）。
- **按列着色（系列）**：选分类列即可把数据按该列拆分对比。
- **数值聚合**：同一 X 分类下有多行时，按 求和/均值/中位/最大/最小/计数/去重计数 聚合。
- **数据筛选**：左侧「数据筛选」按住 Ctrl 多选各分类列取值，实时过滤。
- **✏️ 可编辑数据表**：点左侧「✏️ 数据表」打开底部数据表，双击单元格修改、点行号选中行可删、可加行；修改后图表**实时更新**。
- 支持中文/英文表头，自动识别数值列与分类列。

## 打开文件异常排查

若点「打开 Excel/CSV」后提示「读取失败」，多为以下情形（现已自动处理其中大部分）：
- **旧版 `.xls`**：已内置 xlrd，正常支持。
- **CSV 编码**：自动尝试 UTF-8 / GBK 等，一般无需手动。
- **多工作表 Excel**：自动选择第一个含数据的表。
- 若仍报错：文件可能已损坏、被加密，或正被 Excel 占用未关闭（请先关闭再打开）。报错会给出具体中文原因。

## 安装说明

- 已把 Python 运行环境独立装入 `venv\`（位于本目录），**不污染系统**。
- 依赖：`pywebview`（原生窗口）、`pandas`（数据解析）、`openpyxl`、`plotly`（本地 JS，离线渲染）。
- 系统需已安装 **Microsoft Edge WebView2 运行时**（Win10/11 一般自带）。

## 目录结构

```
ChartCanvas/
├── 启动ChartCanvas.bat      # 一键启动
├── app/
│   ├── main.py              # 后端：文件选择、Excel/CSV解析、聚合、序列化
│   ├── sample_data.py       # 生成演示数据（可选）
│   ├── test_backend.py      # 后端冒烟测试
│   ├── sample_data/         # 演示数据
│   └── web/                 # 前端
│       ├── index.html       # 三栏界面
│       ├── style.css
│       ├── app.js           # 绘图与自定义逻辑（Plotly.js）
│       └── lib/plotly.min.js# 本地离线图表库
└── venv/                    # 独立 Python 环境
```

## 开发备注

- 后端数据接口返回**原始列数组**，聚合统一在前端 JS 完成（`pivotXY` / `AGG`），便于扩展。
- 验证：`venv\Scripts\python.exe app\test_backend.py` 跑后端链路测试；
  设置环境变量 `CHART_DEMO=1` 后启动可自动载入示例并对全部 11 类图自检。
